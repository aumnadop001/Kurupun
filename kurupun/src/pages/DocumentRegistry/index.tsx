import React from 'react';
import { Container } from '@mui/material';
import TableDocument from '../../components/Table';
const DocumentRegistry: React.FC = () => {
  return (
    <Container maxWidth="md">
      <TableDocument />
    </Container>
  );
};

export default DocumentRegistry;
